<UserControl x:Class="WorldCupWPF.UserControls.PlayerCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WorldCupWPF.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="60" d:DesignWidth="50"
             Opacity="0">
    
    <UserControl.Resources>
        <Storyboard x:Key="FadeInAnimation">
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                           From="0" To="1" Duration="0:0:0.5">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>
    </UserControl.Resources>
    
    <UserControl.Triggers>
        <EventTrigger RoutedEvent="UserControl.Loaded">
            <BeginStoryboard Storyboard="{StaticResource FadeInAnimation}"/>
        </EventTrigger>
    </UserControl.Triggers>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="29*"/>
            <ColumnDefinition Width="21*"/>
        </Grid.ColumnDefinitions>
        <Image x:Name="imgIcon" HorizontalAlignment="Left" Height="40" Margin="5,0,0,0" VerticalAlignment="Top" Width="40" Grid.ColumnSpan="2"/>
        <Label x:Name="lblShirtNumber" Content="#" HorizontalAlignment="Left" VerticalAlignment="Top" FontSize="8" Margin="0,39,0,0" FontWeight="Bold" Background="Black" Foreground="DarkMagenta"/>
        <Label x:Name="lblName" Content="Ime" HorizontalAlignment="Left" Margin="20,0,0,0" VerticalAlignment="Bottom" FontSize="11" FontWeight="Bold" Background="Black" Foreground="DarkMagenta" Grid.ColumnSpan="2" Height="21"/>
    </Grid>
</UserControl>
